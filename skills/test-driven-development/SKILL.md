---
name: test-driven-development
description: 在帧芯开发工作流中实现Bug修复、业务逻辑、状态、API、数据、权限、计费或公共合同时，用于先证明测试会失败，再写最小实现并重构；纯视觉和简单配置不强制使用。
---

# 测试驱动开发

## 核心规则

```text
新实现先看到测试因目标行为而失败，再写对应生产代码；接手已有实现按下文补检错证据。
```

接手时实现已经存在，不为补齐顺序撤回或重写正确代码。先按行为合同补测试，可在隔离副本中对修复前版本或受控缺陷运行，证明断言确实能检错，再验证当前实现；不得改动用户工作区来制造失败。区分测试先行与事后补证，不能冒称此前完成了TDD，也不能只写复述实现的断言。

## 适用与例外

严格使用：Bug、业务规则、状态机、API、数据变换、权限、计费、公共接口和可回归的工程行为。

不强制失败测试先行：纯CSS、布局、文案、静态资源、fixture、生成文件和简单配置。它们仍需真实页面、交互、Golden、lint、typecheck或构建验证。

Spike或一次性探索代码只有在明确标为丢弃时可跳过；用户批准保留为生产代码后，按上述已有实现补证方式证明测试有效，并完成相同质量和验收要求，不为流程重新写一遍。

## RED-GREEN-REFACTOR

### RED：写一个最小失败测试

- 一次只描述一个可观察行为；
- 使用真实接口，除非外部边界确实需要mock；
- 名称说明条件和结果；
- Bug修复先写能重现Bug的测试。

### 验证RED

运行精确测试命令并确认：

- 测试失败而不是测试代码报错；
- 失败原因正是缺失行为；
- 新行为的RED测试直接通过时，检查是否已有实现或断言无效；已有实现按补证规则处理，不能仅凭测试绿色判断其无效。

### GREEN：最小完整实现

只写让当前行为正确通过的代码，不顺手加入未来选项、通用框架或无关重构。

### 验证GREEN

重新运行同一测试，确认通过；再运行受影响的邻近测试。不得只看进程退出或截断输出。

### REFACTOR

在保持测试绿色的前提下改善命名、重复和边界。每次小改后重新运行相关测试。

## 好测试

详细判断见 [writing-good-tests.md](references/writing-good-tests.md)。优先验证行为和合同；避免只验证mock调用次数、私有实现、常量复制或测试专用生产分支。

## 卡住时

- 不知道怎么测试：先写希望存在的调用接口，暴露设计问题；
- 测试准备过大：接口可能耦合过重，先缩小依赖；
- 必须mock所有对象：考虑依赖注入和边界抽离，但只做本次需要的改动；
- 测试不稳定：使用系统化调试和基于条件的等待，不增加任意sleep。

完成前记录测试文件、RED命令与失败原因、GREEN命令与结果。没看到RED就不要声称完成了TDD。
