---
name: verification-before-completion
description: 在帧芯开发工作流准备声称功能完成、Bug修复、测试通过、构建成功或可合并前，用于运行能直接证明该主张的最新验证并检查完整输出。
---

# 完成前验证

## 铁律

```text
没有当前回合的新鲜验证证据，就不能声称完成、修复或通过。
```

代理报告、历史输出、推测和“改动很小”都不是证据。

## 验证函数

在说任何成功结论前：

1. 明确该结论需要什么命令或可观察行为才能证明；
2. 运行完整、准确且覆盖当前代码的验证；项目已配置质量门禁时，普通Task运行相关`quality:fast`，Plan收尾运行`quality:pr`；
3. 阅读退出码和完整相关输出，确认没有被截断、跳过或只运行了子集；
4. 将结果与主张逐项对应；
5. 证据不支持且Task因此转 `REWORK/BLOCKED` 时，按 [failure-evidence-contract.md](../product-spec-governance/references/failure-evidence-contract.md) 记录；预期TDD RED不记录；
6. 报告实际状态和失败，不使用模糊乐观措辞。

## 常见主张与证据

- “测试通过”：项目适用的测试命令退出码为0，失败数为0；
- “类型正确”：实际typecheck命令成功；
- “构建成功”：从干净或项目规定状态完成build；
- “项目门禁通过”：`.codex/quality-gates.json`对应档位的真实命令退出码为0，没有把缺少工具伪装成跳过；
- “Bug已修复”：原复现路径或回归测试先失败后通过；
- “UI完成”：真实入口、目标视口和关键状态与Golden或验收合同一致；
- “需求完成”：逐条核对Spec与Plan Acceptance，不只看测试；
- “可以合并”：上述证据加独立Review和产品验收均满足。

## 复用证据

不要让开发者、主任务和Reviewer机械重复运行同一份昂贵验证。可复用证据必须同时满足：代码未变化、命令完整、输出可检查、环境相同且时间足够新。任何一项不满足就重新运行。

代码、接口、状态或数据结构发生任何改动后，受影响Task和验收项的既有`PASS`证据立即标为`SUPERSEDED`，原Plan中的`Product Acceptance: PASS`回写为`PENDING`，必须重新验证；若改动发生在Review或完整验证之后，相关证据也必须覆盖最新diff。未完成真实页面验收时，报告只能写“开发完成，待真页验收”，不能写“已修复”或“已完成”。

## 红旗

- “应该没问题”；
- 只跑lint就说build通过；
- 只看最后几行；
- 子智能体说通过所以直接相信；
- 修复后没有重新运行；
- 用单元测试替代真实入口产品验收。
